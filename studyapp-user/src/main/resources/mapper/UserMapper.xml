<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studyapp.user.mapper.UserMapper">

    <!-- 增加学习天数 -->
    <update id="incrementStudyDays">
        UPDATE user SET study_days = study_days + 1, update_time = NOW() WHERE id = #{userId}
    </update>

    <!-- 增加做题数 -->
    <update id="incrementTotalQuestions">
        UPDATE user SET total_questions = total_questions + #{count}, update_time = NOW() WHERE id = #{userId}
    </update>

</mapper>
